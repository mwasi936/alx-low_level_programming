#include "main.h"

/**
 * _memcpy - copies @n bytesfrom memory area @src
 * @n: function copies
 *@src: bytes form memory area
 *@dest: to memory area
 *Return: a pointer to @dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int = 0;

	while (i < n)
	{
		dest[i] = src[i];
		i++;
	}
	return(desyt)
}

